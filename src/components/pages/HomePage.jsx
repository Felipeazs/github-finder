import React, { Fragment } from 'react';

import UserResults from '../users/UserResults';

const HomePage = () => {
	return (
		<Fragment>
			<UserResults />
		</Fragment>
	);
};

export default HomePage;
